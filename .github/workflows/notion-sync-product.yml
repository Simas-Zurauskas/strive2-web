# yaml-language-server: $schema=https://json.schemastore.org/github-workflow.json
name: 'Knowledge Base: Sync - Product'

on:
  push:
    branches: [master]
  workflow_dispatch:

jobs:
  sync-product:
    uses: Simas-Zurauskas/strive2-ci/.github/workflows/notion-sync-product.yml@master
    secrets: inherit
