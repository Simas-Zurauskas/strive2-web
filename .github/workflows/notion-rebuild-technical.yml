# yaml-language-server: $schema=https://json.schemastore.org/github-workflow.json
name: 'Knowledge Base: Rebuild Technical'

on:
  workflow_dispatch:

jobs:
  rebuild:
    uses: Simas-Zurauskas/strive2-ci/.github/workflows/notion-rebuild-technical.yml@master
    secrets: inherit
